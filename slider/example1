let r = 100;
let slider;

function setup() {
  createCanvas(600, 400);

  slider = createSlider(0, 200, 100);

}

function draw() {
  background(220);
  
  let r = slider.value();
  circle(width/2,height/2,r);
}
